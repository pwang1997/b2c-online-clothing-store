/**
 * Requirements:
 *  1. Header manages user state(guess/logged user)
 *  2. Header contains a search bar to query products.
 *  3. Header has an OAuth login button.
 *  4. Header has shopping cart link.
 *  5. Header has links to various product categories.
 */